[project]
name = "ember"
version = "1.0.0"
description = "A simple BPE based Tokenizer"
readme = "README.md"
authors = [
    { name = "Shishir-Kc", email = "shishir.khatri.kc@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "datasets>=5.0.1",
]

[project.scripts]
ember = "ember:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
